package com.capics.repository;

import com.capics.entity.AiAgentConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AiAgentConfigRepository extends JpaRepository<AiAgentConfig, Integer> {
}
